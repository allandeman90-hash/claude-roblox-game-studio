# Handoff — session 2026-08-31 → 09-01

**29 commits sur master (NON pushés)** — de `73264fb` à `HEAD`.

## Terminé

| Track | Détail |
|---|---|
| **A** assets | 208 sprites uploadés (Open Cloud), Decal→Texture résolus, `AssetMap.luau` (208 ids). `ZoneConfig` = 12 couches × 6 monstres. `CombatClient` : fonds `bg_zoneN` peints. **Rojo installé** (`C:\Users\Allan\.rojo\bin\rojo.exe`, `serve.project.json` code-only). Clé Open Cloud dans `C:\Users\Allan\.roblox\open-cloud.env` (hors repo). |
| **B** sécu (B1-B6) | dev-reset + revive payante supprimés · rate-limit tous remotes + fix exploit `restart` · sauvegardes découplées + heartbeat de lock anti-dupe + BindToClose 25s · `ProcessReceipt` idempotent + `ProductConfig` (12 SKU, ids=0) + `ReceiptService.server.luau` · `RewardService.luau` (resolver max() plafonné ×3) + fermeture session shop · 4 bugs |
| **D** chiffres (D1-D6) | `GameConfig` : `ClassGrowth` (6 tables), courbe XP A `round(6+0.5·n^1.10)`, `refPointsPerLevel 4.7`, `critRate LUK/2500`, `Nightmare`, `SeasonPass`, `Rewards`, `Persistence`, `Security`, `levelGap` (dealtStep **0.035**), `Enemy.levelPerKm 3.5`. `EquipmentConfig` : 50 armes, 104 armures, 12 SetIdentities, bande rareté **large 1.0/1.5/2.2/3.5/6.0**, DropRates/BossLootTable en config. `DamageService` : pénalité d'écart de niveau. Docs : `design/economy/D1-stat-growth.md`, `D6-playthrough-balance.md`. |
| **E** narratif (E1-E4) | `design/narrative/` : `la-descente.md` (bible, 12 Gardiens), `boss-dialogues.md`, `layer-cards.md`, `codex-entries.md` (72+12). |

## En cours — Track C (GDD système), 6/23 écrits

Écrits : `core-gameplay`, `combat` (C1) · `progression`, `talents`, `subclass`, `rebirth` (C2) · `abilities`, `boss-mechanics` (C3) · `nightmare` (C4 partiel).

**À REPRENDRE : C4 = écrire `economy-gdd.md`** (l'agent a crashé après `nightmare-gdd.md`). Décisions déjà prises pour economy-gdd :
- or = jeu, sink dominant = Rebirth (Q64). Gemmes = premium cosmétique/confort seulement.
- Q61(b) « or → paliers Cauchemar » ABANDONNÉ. Puits gardés : cosmétiques en or, ligne de luxe au feu de camp, **Reforge** (objet monte zone effective +1, ≤ zone max, coût or croissant, `reforgeLevel[itemKey]`).
- boutique feu de camp = **5 objets** (arme + 4 armures de TA voie) + 1 ligne de luxe occasionnelle. Marchand ambulant ~15%/cluster avant km 50.
- respec stats = **250 × (points alloués)**, pas d'escalade, aussi Robux.
- gemmes f2p ~300-500/saison (piste gratuite pass + VIP 10/j + Pack Départ 150) — ballpark, fin → H6.

**Puis C5-C8** (voir `docs/plan/00-plan-complet.md` §D Track C) :
- C5 : `/gdd pets`, `/gdd codex`, `/gdd inventory`
- C6 : `/gdd campfire`, `/gdd daily-reward`, `/gdd missions`, `/gdd daily-dungeon`, `/gdd raid`
- C7 : reste (voir systems-index — `weapons`, `armor-sets`, `trading`, `social`, `leaderboards`, `ftue`, `settings`, `accessibility`…)
- C8 : lot final + `/design-review` sur tous (gate Track C)

## Dette / follow-ups

Tout est dans **`docs/plan/06-followups.md`**. Points chauds :
- **`EnemyService.rollEnemy` ne propage pas `famille`/`petRole`/`magic`** (bloque codex, pets dérivés, mitigation magique caster) → C5/G3/G6.
- **`PROFILE_VERSION → 2`** (B3/B4/B5/C2 ont ajouté des champs en additif) → G1.
- **IDs produits/gamepass = 0** dans `ProductConfig` / `Rewards.permanentPasses` → Track I (Creator Dashboard).
- Champs profil écrits mais non consommés (`premiumSeason`, `passTierCredits`, `unclaimedProducts`, `fusionTokens`, `renameCredits`, `boosts`) → H/I.
- `st.nightmareTier` lu par RewardService mais jamais posé → G9.
- Maquettes manquantes (Talents, encart feu de camp Sous-classe/Rebirth) → Track F.

## Reprise

1. `git push` si voulu (29 commits en attente).
2. Rojo : relancer `C:\Users\Allan\.rojo\bin\rojo.exe serve serve.project.json` + Connect dans Studio.
3. Relancer Track C via un game-designer sur `economy-gdd.md` puis C5-C8.
4. Après Track C : `/design-review`, puis tracks F (UI/UX) → G (implémentation gameplay) → H (pass) → I (monétisation) → J (audio) → K (QA/perf/sécu) → L (release).
