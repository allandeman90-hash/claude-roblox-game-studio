# Handoff — session 2026-09-03 (reprise après désordre)

## Ce qui s'est passé

Entre le 1er et le 3 sept., du travail s'est accumulé **sans aucun commit** (un
2e agent « Codex » tournait en parallèle, des docs de plan ont été supprimées).
Cette session a remis de l'ordre :

1. **Filet de sécurité** : branche `wip/reprise-2026-09-03` (commit `c476db0`)
   = snapshot COMPLET des 64 fichiers non commités (tout est là, rien perdu).
2. `master` a été ramené propre sur `238d82d`, puis reconstruit en commits nets.
3. Toolchain Codex (`.codex/`, `.agents/`, `AGENTS.md`) supprimé — **Claude Code seul**.

## État de `master` (2 commits ahead of origin, NON pushés)

| Commit | Contenu |
|---|---|
| `d0617ee` docs(gdd) | Track C : 12 GDD système (economy, pets, codex, inventory, missions, campfire, daily-reward, daily-dungeon, raid, season-pass, ui-ux, onboarding) + `systems-index` v1.4 + `design/specs/inventory-implementation-spec.md` + plan sprint inventaire + audit token-efficiency. **Track C ~18/23.** |
| `8e4ae0b` feat(gameplay) | Système d'inventaire complet (InventoryService/Client/Screen, BagFullPrompt, AccessibilityScreen, StyleConstants) + Track G : CombatServer/CombatClient réécrits, EquipmentService/StatsService/ShopService/EnemyService/LootService/PlayerDataService/ReceiptService retouchés, GameConfig/Types/AssetMap étendus. **⚠️ NON TESTÉ EN STUDIO.** |
| `19a590d` chore | `.gitignore` (ignore `/Réference/` + `_rejected-moderation/`) + ce handoff |
| `2a1c483` chore | **Suppression du wiki** (`wiki/`, skills `/wiki-*`, agent `wiki-curator`, hook `validate-wiki.sh`) + QCM périmé (`questions-jeu.md`, `reponses-consolidees.md`). CLAUDE.md allégé : n'auto-charge plus `wiki/SCHEMA.md`, `coordination-rules.md`, `context-management.md` → ~950 lignes/session en moins. |

Tracks **A / B / D / E : terminés** (voir `07-handoff-2026-09-01.md`).

## À FAIRE ensuite — dans l'ordre

### 0. TEST STUDIO FAIT (2026-09-03) — `8e4ae0b` validé au runtime
Zéro erreur Luau. Combat / mort / game-over / restart / inventaire (addItem →
equipItem) OK. Migration `PROFILE_VERSION 2` OK. UI paysage rend correctement.

**FAIT depuis (commits poussés) :**
- `40517d7` puis `5661180` — **FTUE complet** (`FtueService` + `FtueClient`).
  Tuto court : move → 1er kill (Épée en bois) → équiper → « le jeu est dur »
  → familier Fée (dps, ×0.5, sprite `pet_dps`). Testé bout en bout en Studio.
- `bafd856` — 6 sprites familiers uploadés (Studio MCP) + AssetMap. Modération passée.
- Robux revive Game Over = **VOULU** (mort près du boss). Rien à faire.

**Reste :**
- **Difficulté trop punitive.** Héros mort à km 1.4 au niv 1 en test. Cible =
  « WoW Classic » (grind solo d'un mob de son niveau confortable, danger si
  multi-mobs ou niveau supérieur). Knobs : `GameConfig.levelGap.dealtStep 0.035`,
  `Enemy.levelPerKm 3.5`, courbe HP/ATK. → passe `/balance-check` + economy-designer.
- DataStore KO en Studio = réglage *Game Settings → Security → Enable Studio
  Access to API Services*, géré en mode « degraded ». À activer pour tester la persistance.
- `upload.py` : le dossier `pets/` est hors `SCAN_DIRS` et son cache ne connaît
  pas les 6 sprites uploadés via MCP. Ne PAS lancer `upload.py` tel quel (il
  réécrit tout `AssetMap` depuis un cache d'ids Decal périmés).

### 1. Valider le commit gameplay en Studio (BLOQUANT avant push)
- Lancer Rojo : `C:\Users\Allan\.rojo\bin\rojo.exe serve serve.project.json` → Connect dans Studio.
- Play mode : vérifier que tous les modules `require` sans erreur (console).
- Tester : combat de base, ramassage loot, ouverture inventaire, équiper un objet, boutique.
- Si ça casse : `git revert 8e4ae0b` OU corriger, puis re-tester. Le code source complet reste dans `c476db0` en secours.

### 2. Récupérer les assets restés dans `c476db0` (session dédiée)
Encore SEULEMENT dans la branche wip, à trier :
- `assets/images/equipment/` (6,4 Mo — sprites d'équipement)
- `tools/assetgen/build_starter_layers.py`
- Variantes PNG boss/mob : `-flip`, `_safe-v2`, `_safe-v3`, `-processed` + 3 PNG trackés modifiés (`bigboss_roi_gobelin`, `boss_roi_gobelin`, `mob_c01_ogre_sentier`) → **décider quelle version est canonique** (les `_safe-*` sont les remplacements post-modération).
- `Réference/` : APK + maquettes UI → rester en local (ignoré).

Commande pour ré-extraire un fichier : `git checkout c476db0 -- <chemin>`

### 3. Finir Track C (5 GDD restants)
`leaderboards`, `weapons`, `armor-sets`, `trading`, `social`, `settings`, `ftue`
(voir `systems-index.md`). Puis `/design-review` sur le lot (gate Track C).

### 4. Track G suite (implémentation gameplay)
Dette dans `06-followups.md`. Points chauds : `PROFILE_VERSION → 2` + migration,
`EnemyService.rollEnemy` propage `famille`/`petRole`/`magic`, talents dans
`StatsService.recalc`, `st.nightmareTier` jamais posé, pets dérivés de la famille.

### 5. Puis F (UI/UX) → H (season pass) → I (monétisation, IDs=0) → J (audio) → K (QA/perf/exploit) → L (release)

## Règles (rappel)
- Proposer (diff/plan) avant d'écrire. Commit sur demande, messages `type(scope): desc` en français.
- Après chaque modif `src/` : vérif Studio Play mode.
- Ne pas casser les 5 protections de `design/economy/monetization.md` §2.
- Ne jamais toucher `src/StarterGui/` (RpgGui manuel, hors Rojo).
- **Un seul agent à la fois. Commiter avant de passer le relais.**

## Dette introduite par le nettoyage
- `README.md` / `.claude/docs/agent-roster.md` annoncent encore 50 skills / 36 agents
  (réel : 44 / 35 après retrait du wiki). `/doctor` le signalera. Corriger au passage.
- Les sections `## Wiki Ownership` dans ~10 fichiers `.claude/agents/*.md` sont du
  texte mort inoffensif — à nettoyer si un agent est édité.

## Nettoyage possible plus tard
`git branch -D wip/reprise-2026-09-03` une fois les assets récupérés et le
commit gameplay validé — puis `git gc` pour dégonfler le repo.
