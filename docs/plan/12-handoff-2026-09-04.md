# Handoff — 2026-09-04

**Pour :** session Claude Code après `/clear`.

---

## État

- `master` = **`10dacde`**, tout poussé, working tree propre.
- Untracked (pas nous, ne pas commit) : `.agents/` `.codex/` `AGENTS.md`.
- **Rojo tourne** (relancé, PID variable, port 34872) mais le **plugin Studio
  n'est PAS reconnecté** → rien n'est synchro. **Reconnecter le plugin Rojo
  dans Studio en mode Edit avant tout.** Vérif : `script_grep "levelPerKm"` doit
  montrer `1.15`.

### Fait depuis le dernier /clear (11 commits, tous poussés)

| Commit | Quoi | Testé en jeu ? |
|---|---|---|
| `5fb4646` | fix lock JOB_ID Studio (+ PROFILE_VERSION 3→4) | ✅ rejoin |
| `967e467` | XP familier visible (HUD + inventaire) | ✅ |
| `aebec93` | HUD cartes détachées | ✅ visuel |
| `3d1a8dc` | retinte Shop/Chateau/GameOver + AccessibilityScreen | props seulement |
| `7b10129` | équip/**déséquip** familier + confirmation SUPPRIMER | ✅ |
| `df1b74b` | GAME OVER refait + revive 9 R$ / gratuit dev | ✅ panneau visuel |
| `7283b09` | handoff | — |
| `65ac396` | **fix balance** : falaise niveau zone 1→2 (levelPerKm 3.5→1.15) | ❌ Rojo déco |
| `808ca1e` | **écran de chargement** (`ReplicatedFirst/BootScreen`) + CoreGui off | ❌ Rojo déco |
| `10dacde` | note péremption `D6-playthrough-balance.md` | — |

### NON testé (Rojo déconnecté à la fin) — à valider en jeu

- **`65ac396` balance** : reconnecter Rojo → Play → marcher jusqu'à km 10-12,
  tuer le boss couche 1 (Roi Gobelin niv.10), passer en couche 2, vérifier que
  les mobs sont ~niv.12 (PAS 35). Puis descendre un peu, vérifier la rampe.
- **`808ca1e` boot** : Play depuis un état frais → écran « QUÊTE MINUTE / — La
  Descente — » + barre, fondu après ~1.5 s ; coin haut-droit (PlayerList) vide.
- **`df1b74b` revive / GAME OVER** : vraie mort → panneau, clic « RÉAPPARAÎTRE ·
  gratuit (dev) » → repart où on est tombé, PV pleins.
- **`3d1a8dc` overlays** : Shop au feu de camp (km 50) / Château → retinte OK ?

---

## Où on en est (roadmap `00-plan-complet.md`)

- **Phase 1** (Tracks A–E : assets, code stable, 23 GDD, chiffres, narratif) → faite.
- **Track F — écrans** → presque bouclé :
  - HUD ✅ · Inventaire ✅ · Shop/Château ✅ (retinte) · GAME OVER ✅ ·
    Accessibilité/BagFull ✅ · FTUE ✅ · **écran de chargement ✅ (nouveau)**
  - **Reste : menu titre, création du héros** — pas commencés, **décisions en attente** (voir ci-dessous).
- **Track G** → partiel (scaling pet niveau ✅, XP pet ✅). Compétences/talents/
  sous-classes/Cauchemar/party-3-pets = GDD écrits, pas câblés.
- **Track I** → à peine (revive `df1b74b`). Reste game passes + autres dev
  products + UI boutique. Tous les IDs à `0`.
- **Tracks H, J, K, L** → pas commencés.
- **Prochaine grande porte : GATE PHASE 2** — « compte neuf → chargement → menu
  → création → run guidée → mort → restart → 2ᵉ run sans cul-de-sac ». Bloquée
  par menu titre + création héros.

---

## ⚠️ Décisions en attente (Allan) — avant menu titre / création héros

1. **Y a-t-il un menu titre maintenant ?** La maquette `02-maquettes.md` §01 en
   veut un (logo + JOUER + 6 accès : Talents/Sac/Boutique/Codex/Rang/⚙). Mais
   Codex, Classement, Récompense du jour, Pass saison **n'existent pas** (Track
   H). Options : (a) menu titre avec accès grisés « bientôt », (b) pas de menu
   pour l'instant, on entre direct en jeu (état actuel), le menu vient avec H.
2. **Création du héros** — la maquette §02 dit « COMMENCER donne l'épée tier 1 »
   mais `onboarding-gdd.md` v2.0 (implémenté) dit « l'épée tombe au 1er kill ».
   L'onboarding-gdd gagne (plus récent + implémenté). Donc à la création il
   reste : **voie Guerrier/Mage** + teinte + nom. Questions :
   - La voie sert à quoi aujourd'hui ? Le FTUE grante `epee_courte` (arme
     Guerrier) au 1er kill **quelle que soit la voie**. Faut-il que Mage →
     `baguette_courte` ? (nécessite une arme Mage tier 1 dans `EquipmentConfig`).
   - « teinte » du héros : le sprite héros est unique (paper-doll futur). On
     teinte quoi ? Un `ImageColor3` sur le sprite ? On reporte à plus tard ?
   - Sans création, le joueur est Guerrier par défaut. Est-ce bloquant pour la
     GATE PHASE 2, ou acceptable en v0 ?
3. **`EquipmentConfig.SLOTS` n'a pas `"gants"`** — les gants ne sont pas
   équipables (bug pré-existant). À corriger ?

---

## Méthode (rappel Allan)

- **Combler les trous à sa place** — proposer « tu veux que je fasse ça aussi ? »,
  ne pas juste signaler.
- Allan = 1er jeu, 1re fois Claude Code, jeu **full GUI** (pas 3D). Expliquer.
- Screenshot avant/après, OK d'Allan avant chaque commit, commits FR, push si demandé.
- **Ne PAS lancer `upload.py`** (écrase `AssetMap.luau`).
- Dev products : Allan crée les IDs dans le dashboard **à la toute fin**.
- Autonomie : OK pour enchaîner les tâches **débloquées et sûres** sans demander,
  mais s'arrêter net sur une vraie décision de design et faire un récap.

---

## Références

- `docs/plan/00-plan-complet.md` §C (tracks + graphe) · `05-ship-roadmap.md` (phases)
- `docs/plan/02-maquettes.md` (wireframes) · `03-sur-tous-les-ecrans.md`
- `design/gdd/onboarding-gdd.md` (FTUE, autoritaire) · `master-gdd.md`
- `src/ReplicatedStorage/Shared/StyleConstants.luau` (palette, `makeButton`)
- Mémoire : `minutrpg-build-progress`
