# Handoff — 2026-09-04

**Pour :** session Claude Code après `/clear`.

---

## État

- `master` = **`3792778`**, tout poussé, working tree propre.
- Untracked (pas nous, ne pas commit) : `.agents/` `.codex/` `AGENTS.md`.
- **Rojo tourne** (port 34872). Vérifier le plugin Studio connecté (Edit) :
  `script_grep "createHero"` doit répondre.
- Profil Studio (uid 7860387011) : **version 5**, voie=mage, heroName=Sylve,
  teinte=foret, FTUE done. Pour repartir neuf : `RemoveAsync("u_7860387011")`
  en Edit → prochain Play = boot → menu → création.

---

## Fait cette session (17 commits, tous poussés)

**Track F — écrans — TERMINÉ.**

| Commit | Quoi | Vérifié en jeu |
|---|---|---|
| `5fb4646` | fix lock JOB_ID Studio | ✅ |
| `967e467` | XP familier visible | ✅ |
| `aebec93` | HUD cartes détachées | ✅ |
| `3d1a8dc` | retinte Shop/Chateau/GameOver | props |
| `7b10129` | équip/**déséquip** familier + confirm SUPPRIMER | ✅ |
| `df1b74b` | GAME OVER refait + revive 9 R$ / gratuit dev | ✅ panneau |
| `65ac396` | **fix balance** falaise niveau (levelPerKm 3.5→1.15) | ✅ km 10→niv 12 |
| `808ca1e` | **écran de chargement** + CoreGui off | ✅ |
| `10dacde` | note péremption D6 | — |
| `3e435fe` | **menu titre + création du héros** | ✅ bout-à-bout |
| `3cbb3db` | teinte du héros appliquée (sprite) + aperçu live | ✅ |
| `3792778` | polish menu titre (logo, héros sur la ligne, grille) | ✅ |

### GATE PHASE 2 — flux vérifié bout-à-bout

Profil neuf → boot → menu titre → JOUER → **création** (voie Mage → arme
« Badine fêlée », teinte foret → sprite vert, nom « Sylve ») → COMMENCER →
profil v5 → FTUE (marche→kill→équipe→œuf→éclosion→briefing→done) → jeu.
Joueur qui revient → JOUER → jeu direct, le héros teinté est sur la ligne du menu.

**⚠️ Reste à voir en conditions réelles** (les outils MCP screenshot/input ont
été instables ; pour forcer un GUI : `RunService.RenderStepped` qui re-set
`.Visible`) :
- Le revive (`df1b74b`) après une **vraie mort** + un **vrai achat** DevProduct.
- Les overlays Shop/Château **peuplés** (feu de camp km 50 / bouton Château).
- Un cycle complet mort → GAME OVER → RECOMMENCER → 2ᵉ run.

---

## Où on en est (roadmap `00-plan-complet.md`)

- **Phase 1** (Tracks A–E) → faite.
- **Track F** (écrans) → **TERMINÉ**.
- **GATE PHASE 2** → le flux marche ; à valider par un playthrough complet d'Allan.
- **Track G** (systèmes combat/progression) → partiel : scaling pet niveau ✅,
  XP pet ✅, voie/classe câblée ✅. **Reste** : compétences (3 pouvoirs,
  `abilities-gdd.md`), talents (panneau existe, points pas câblés partout),
  sous-classes au R5 (`subclass-gdd.md`), Cauchemar, party 3 pets.
- **Track H** (rétention) → **pas commencé**. Récompense quotidienne, missions,
  codex, classements, analytics. C'est ce qui rend les 4 raccourcis grisés du
  menu titre réels (Codex, Rang) + la récompense/pass de la maquette §01.
- **Track I** (monétisation) → juste le revive. Reste 8 game passes + ~6 dev
  products + UI boutique. **Tous les IDs à `0`** — Allan les crée à la fin.
- **Tracks J/K/L** → pas commencés.

### Prochaine priorité — au choix d'Allan

- **A. Track H** (rétention) — le GDD dit que c'est ça qui fait revenir un
  joueur. Débloque les raccourcis du menu. Gros mais bien spécifié
  (`daily-reward-gdd`, `missions-gdd`, `codex-gdd`, `leaderboards-gdd`).
- **B. Track G** (compétences + talents + sous-classes) — approfondit le combat.
- **C. Polish/QA de ce qui existe** — playthrough complet, tuning balance
  (refaire `/balance-check`, D6 est périmé), les follow-ups de `06-followups.md`.

---

## Trous / dette connus

- **`EquipmentConfig.SLOTS` n'a pas `"gants"`** → gants pas équipables. À corriger.
- **`D6-playthrough-balance.md` périmé** (levelPerKm + courbe XP ont changé) —
  refaire un `/balance-check` avant la GATE.
- Aperçu du héros à la création : sprite + teinte OK, mais pas d'anim.
- Menu titre : raccourcis grisés (Track H), ligne de sol fine, layout perfectible.
- `CombatClient` frôle la limite Luau 200 locals (bloc GAME OVER dans un `do`).
  Toute grosse feature client : même précaution.
- FTUE : pendant les tests, elle s'est parfois auto-complétée (input clavier
  resté enfoncé entre replays + `currentStep` local du FtueClient). Pas un bug
  du code neuf mais à surveiller.

---

## Méthode (rappel Allan)

- Suivre le **master GDD** (= la synthèse du questionnaire des 119 Q).
- Combler les trous à sa place — proposer, ne pas juste signaler.
- Allan = 1er jeu, 1re fois Claude Code, jeu **full GUI**. Expliquer.
- Screenshot avant/après, OK d'Allan avant chaque commit, commits FR, push si demandé.
- Enchaîner les tâches débloquées et sûres sans demander ; s'arrêter net sur
  une vraie décision de design + récap.
- **Ne PAS lancer `upload.py`**. Dev products : Allan, à la fin.

---

## Références

- `docs/plan/00-plan-complet.md` §C · `05-ship-roadmap.md`
- `design/gdd/master-gdd.md` (autoritaire) · `02-maquettes.md` · `03-sur-tous-les-ecrans.md`
- `design/gdd/onboarding-gdd.md` · `abilities-gdd.md` · `subclass-gdd.md` ·
  `daily-reward-gdd.md` · `missions-gdd.md` · `codex-gdd.md` · `leaderboards-gdd.md`
- `src/ReplicatedStorage/Shared/StyleConstants.luau` · `GameConfig.Hero`
- Mémoire : `minutrpg-build-progress`
