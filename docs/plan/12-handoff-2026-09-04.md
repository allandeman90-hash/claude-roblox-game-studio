# Handoff — 2026-09-04 — post équip/déséquip pet + GAME OVER refait + revive

**Pour :** session Claude Code après `/clear`. Suite de `11-handoff-2026-09-03-soir.md`.

---

## État du repo

- `master` = **`df1b74b`**, working tree **propre**, **tout poussé** sur origin.
- 6 commits la session d'avant (nuit du 3→4) :
  - `5fb4646` fix lock JOB_ID Studio (+ `PROFILE_VERSION` 3→4)
  - `967e467` XP familier visible (HUD + inventaire) — **vérifié en jeu**
  - `aebec93` HUD cartes détachées (bordures 96,96,101, texte 16)
  - `3d1a8dc` retinte Shop/Chateau/GameOver + fix AccessibilityScreen
  - `7b10129` équip/déséquip familier + confirmation SUPPRIMER — **vérifié en jeu**
  - `df1b74b` GAME OVER refait + revive 9 R$ / gratuit dev — **panneau vérifié visuellement**
- Untracked (pas nous, ne pas commit) : `.agents/` `.codex/` `AGENTS.md`.

### ⚠️ Rojo N'EST PAS lancé

Le process de sync est tombé en fin de session précédente. Relancer :
```
~/.rojo/bin/rojo.exe serve serve.project.json
```
en tâche de fond, puis **reconnecter le plugin Rojo dans Studio en mode Edit**
(pas Play — sinon « Http requests can only be executed by game server »).
Vérif : `netstat -ano | grep 34872`.

### Setup

1. Rojo (ci-dessus).
2. `mcp__Roblox_Studio__list_roblox_studios` (le `studio_id` change).
3. Vérifier la sync : `script_grep` un marqueur récent (ex. `configureReviveBtn`
   ou `reviveInPlace`) **avant** tout Play.
4. DataStore **fonctionne** en Studio (API activée) — le profil persiste. Pour
   repartir d'un profil neuf : `store:RemoveAsync("u_7860387011")` en mode Edit
   (`DataStoreService:GetDataStore("FoG_PlayerData_v1")`).

### ⚠️ Outils MCP instables

`screen_capture` / `user_mouse_input` / `user_keyboard_input` ont hang 120 s+
plusieurs fois cette session (Studio pas au premier plan ?). `execute_luau`
marche toujours. Pour forcer un overlay visible malgré `updateUI` qui le
re-cache chaque tick : une connexion `RunService.RenderStepped` qui re-set
`.Visible = true`, puis screenshot dans la fenêtre.

---

## ⭐ EN PREMIER — Allan doit créer le DevProduct revive

Le bouton **RÉAPPARAÎTRE** de l'écran GAME OVER (commit `df1b74b`) :
- Dev (`GameConfig.DevUserIds = { 7860387011 }`) → **gratuit** (`devRevive`).
- Autres → `MarketplaceService:PromptProductPurchase(reviveRun id)`, **9 R$**.
- Tant que `ProductConfig.ids.reviveRun == 0` → bouton grisé « bientôt ».

**Action Allan** : créer un Developer Product à **9 R$** dans le Creator
Dashboard, coller son id dans `src/ReplicatedStorage/Shared/ProductConfig.luau`
→ `ids.reviveRun`. Puis tester l'achat en jeu.

---

## NON VÉRIFIÉ (à valider en jouant)

1. **Logique serveur du revive** — `reviveInPlace` (CombatServer), les handlers
   `devRevive` / `claimRevive`, le chemin `ReceiptService` (`effect.kind ==
   "revive"` → `profile.pendingRevives++`). Besoin d'une **vraie mort** (le
   panneau) puis clic dev-revive → doit repartir où on est tombé, PV pleins,
   `st.gameOver = false`. Puis un **vrai achat** du DevProduct.
2. **Overlays Shop / Chateau peuplés** — retinte `3d1a8dc` vérifiée par lecture
   de propriétés seulement (fond `14,15,19`, strokes `96,96,101`). Les voir en
   vrai : Shop au feu de camp, Chateau via son bouton.
3. **GAME OVER en conditions réelles** — le panneau a été vu en forçant
   `.Visible` ; jamais déclenché par une vraie mort (`data.gameOver = true` →
   `showGameOver` tourne, `hideGameOver` non).

---

## Trous / dette connus

- **`EquipmentConfig.SLOTS` n'a pas `"gants"`** (`{ arme, casque, plastron,
  jambieres, bottes, pet }`) — bug pré-existant. `EquipmentService.equip
  (player, "gants", …)` renvoie « invalid slot ». À corriger si les gants
  doivent être équipables (le panneau ÉQUIPÉ de l'inventaire liste déjà
  « Gants »).
- **`ProcessReceipt` : aucun DevProduct n'a d'id réel** (tous à 0 dans
  `ProductConfig.ids`) — normal, Track I. Le revive suit le même chemin.
- Écrans encore jamais retravaillés : `BagFullPrompt` (déjà sur `BaseColors`,
  OK a priori), et le reste du brief `09` si pertinent.
- Nettoyage différé (voir `10-handoff` §Nettoyage) : nœuds morts de `RpgGui`
  (dont `GameOverWindow` maintenant caché mais toujours dans le `.rbxl` /
  `RpgGui.gui.json`), bloc `equipOverlay` mort dans `CombatClient` (~L1560),
  en-tête périmé de `FtueService.luau`.
- **`CombatClient` frôle la limite Luau de 200 locals** (le bloc GAME OVER a
  dû être enveloppé dans un `do`). Toute nouvelle feature client volumineuse :
  même précaution (bloc `do`, ou nouveau ModuleScript).

---

## Méthode (rappel Allan, 2026-09-03)

- **Combler les trous à sa place** — proposer « tu veux que je fasse ça
  aussi ? » tout de suite, ne pas laisser une liste de trous « à signaler
  plus tard ».
- Allan = 1er jeu, 1re fois avec Claude Code, jeu **full GUI** (pas 3D). Ne pas
  surestimer ses connaissances ; expliquer.
- Screenshot avant/après via MCP, OK d'Allan avant chaque commit.
- Commits en français (`type(scope): description`), push seulement si Allan le dit.
- Compte Roblox **non banni** — uploads OK, mais **ne PAS lancer `upload.py`**
  (écrase `AssetMap.luau`) ; passer par `mcp__Roblox_Studio__upload_image` +
  édition manuelle.

---

## Références

- `docs/plan/11-handoff-2026-09-03-soir.md` · `docs/plan/10-handoff-2026-09-03.md`
- `docs/plan/09-ui-refonte-brief.md` — brief UI d'origine
- `src/ReplicatedStorage/Shared/StyleConstants.luau` — palette (`BaseColors`,
  `Rarities`, `ButtonVariants`, `makeButton`)
- `Réference/mon ui final.png` — cible visuelle HUD
- Mémoire : `minutrpg-build-progress`, `minutrpg-combat-art-direction`,
  `studio-place-out-of-sync`, `roblox-open-cloud-credentials`
